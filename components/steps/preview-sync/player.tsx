import type { RefObject } from "react";
import type { SyncSegment } from "@/components/steps/preview-sync/types";

export function PreviewPlayer({ activeSegment, playing, time, total, videoSrc, videoRef, onToggle, onTimeUpdate, onPlay, onPause, onEnded, onLoadedMetadata }: { activeSegment?: SyncSegment; playing: boolean; time: number; total: number; videoSrc?: string; videoRef: RefObject<HTMLVideoElement | null>; onToggle: () => void; onTimeUpdate: (time: number) => void; onPlay: () => void; onPause: () => void; onEnded: () => void; onLoadedMetadata: (duration: number) => void }) {
  return <div className="relative aspect-video overflow-hidden rounded-xl bg-slate-900 shadow-lg ring-1 ring-slate-200 dark:bg-slate-950 dark:ring-slate-700">{videoSrc ? <video ref={videoRef} src={videoSrc} className="absolute inset-0 size-full object-cover" preload="metadata" onTimeUpdate={(event) => onTimeUpdate(event.currentTarget.currentTime)} onPlay={onPlay} onPause={onPause} onEnded={onEnded} onLoadedMetadata={(event) => onLoadedMetadata(event.currentTarget.duration)} /> : <div className="absolute inset-0 bg-gradient-to-br from-slate-800 via-slate-900 to-teal-950" />}{activeSegment?.overlayAssetPath ? <img src={activeSegment.overlayAssetPath} alt={`Overlay for ${activeSegment.ayah}`} className="pointer-events-none absolute inset-0 size-full object-contain" /> : activeSegment ? <div className="absolute left-1/2 top-5 w-[88%] -translate-x-1/2 animate-[fadeIn_0.15s_ease]"><div className="rounded-xl border border-white/10 bg-black/65 px-5 pb-3 pt-4 shadow-xl backdrop-blur-sm"><p className="mb-2.5 text-right text-2xl leading-[1.9] text-white sm:text-3xl" dir="rtl" style={{ fontFamily: "serif, system-ui", letterSpacing: "0.02em" }}>{activeSegment.arabic}</p><p className="text-center text-xs leading-relaxed text-white/75 sm:text-sm">{activeSegment.translation}</p><div className="mt-2.5 flex items-center justify-between"><span className="text-[10px] font-mono text-white/40">{activeSegment.ayah}</span><span className="text-[10px] font-mono text-white/40">{time.toFixed(1)}s</span></div></div></div> : <div className="absolute inset-0 flex items-center justify-center"><p className="text-xs text-white/30">No overlay · between segments</p></div>}<div className="pointer-events-none absolute inset-0 flex items-end justify-center pb-10"><button type="button" onClick={onToggle} className="pointer-events-auto flex size-12 items-center justify-center rounded-full border border-white/20 bg-white/10 backdrop-blur-sm transition-all hover:bg-white/20 active:scale-95">{playing ? <svg width="16" height="16" viewBox="0 0 24 24" fill="white"><rect x="6" y="4" width="4" height="16" /><rect x="14" y="4" width="4" height="16" /></svg> : <svg width="16" height="16" viewBox="0 0 24 24" fill="white"><polygon points="5 3 19 12 5 21 5 3" /></svg>}</button></div><div className="absolute right-3 top-3 rounded-md bg-black/50 px-2 py-1 text-xs font-mono text-white/80 backdrop-blur-sm">{formatTime(time)} / {formatTime(total)}</div></div>;
}

export function formatTime(value: number) { return `${Math.floor(value / 60)}:${String(Math.floor(value % 60)).padStart(2, "0")}`; }
