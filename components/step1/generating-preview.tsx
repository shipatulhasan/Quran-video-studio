import type { Step1Row } from "@/components/step1/types";

export function Step1GeneratingPreview({ rows, count, progress }: { rows: Step1Row[]; count: number; progress: number }) {
  return <div className="space-y-6 animate-[fadeIn_0.2s_ease]"><div className="space-y-1"><h1 className="text-xl font-semibold tracking-tight text-slate-900 dark:text-slate-100">Generating Overlay Cards</h1><p className="text-sm text-slate-500 dark:text-slate-400">Processing segment {count} of {rows.length}…</p></div><div className="space-y-5 rounded-xl border border-slate-200 bg-white p-8 dark:border-slate-700 dark:bg-slate-900"><div className="flex items-center justify-between text-sm"><span className="font-medium text-slate-700 dark:text-slate-300">Generating card {count} of {rows.length}…</span><span className="font-semibold tabular-nums text-teal-600 dark:text-teal-400">{progress}%</span></div><div className="h-2 overflow-hidden rounded-full bg-slate-100 dark:bg-slate-800"><div className="h-full rounded-full bg-teal-600 transition-all duration-300 dark:bg-teal-500" style={{ width: `${progress}%` }} /></div><div className="grid grid-cols-3 gap-2.5 pt-1 sm:grid-cols-4">{rows.slice(0, count).map((row) => <div key={row.id} className="flex aspect-video items-center justify-center overflow-hidden rounded-lg bg-slate-900 ring-1 ring-teal-500/30 animate-[fadeIn_0.2s_ease] dark:bg-slate-950"><p className="px-2 text-center text-[9px] leading-relaxed text-white/90" dir="rtl">{row.arabic.slice(0, 18)}…</p></div>)}</div></div></div>;
}
